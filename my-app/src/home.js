import React from 'react';
import { Carousel, Card } from 'react-bootstrap';

const Home = () => {
  return (
    <section>
      {/* Carousel */}
      {/* Cards */}
    </section>
  );
};

export default Home;
